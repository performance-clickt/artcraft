{
  "name": "artcraft-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "MCP server that drives a running ArtCraft desktop app through its loopback control server.",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "artcraft-mcp": "dist/index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "test": "tsc -p tsconfig.test.json && node --test \"dist-test/tests/**/*.test.js\""
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.20.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
